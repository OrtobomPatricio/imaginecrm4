# ══════════════════════════════════════════════════════════════════════════════
# ImagineCRM — Metabase Dashboard Update
# Variables de entorno para el script de actualización automática
# Copia este archivo como .env y completa los valores
# ══════════════════════════════════════════════════════════════════════════════

# ── Metabase ──────────────────────────────────────────────────────────────────
METABASE_URL=https://metabase.tuempresa.com

# API Key (recomendado) — Crear en: Admin → Settings → API Keys
METABASE_API_KEY=mb_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# O alternativamente, usuario y contraseña:
# METABASE_EMAIL=admin@tuempresa.com
# METABASE_PASSWORD=tu_contraseña_segura

# ── IDs del dashboard y base de datos ────────────────────────────────────────
# Estos valores los obtienes al ejecutar setup_metabase_dashboard.py
# Si no los configuras, el script los buscará automáticamente por nombre
# (más lento, pero funciona)
METABASE_DASHBOARD_ID=1
METABASE_DATABASE_ID=2

# ── Base de datos MySQL (para re-sincronización de esquema) ──────────────────
DB_HOST=db.tuempresa.com
DB_PORT=3306
DB_NAME=imaginecrm
DB_USER=metabase_readonly
DB_PASSWORD=contraseña_readonly_segura

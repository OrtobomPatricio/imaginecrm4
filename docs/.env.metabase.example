# ══════════════════════════════════════════════════════════════════════════════
# ImagineCRM — Metabase Dashboard Setup
# Copia este archivo como .env y completa los valores antes de ejecutar el script
# ══════════════════════════════════════════════════════════════════════════════

# ── Metabase ──────────────────────────────────────────────────────────────────
# URL base de tu instancia de Metabase (sin barra final)
METABASE_URL=https://metabase.tuempresa.com

# Método de autenticación (elige uno de los dos):

# Opción A (RECOMENDADA): API Key
# Crear en Metabase: Admin → Settings → API Keys → Create API Key
# Asignar al grupo "Administrators" para tener permisos completos
METABASE_API_KEY=mb_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Opción B: Usuario y contraseña de administrador
# (Solo si no puedes usar API Key)
METABASE_EMAIL=admin@tuempresa.com
METABASE_PASSWORD=tu_contraseña_segura

# ── Base de datos MySQL de producción ────────────────────────────────────────
# Host de la base de datos (IP o dominio, accesible desde el servidor de Metabase)
DB_HOST=db.tuempresa.com

# Puerto MySQL (por defecto 3306)
DB_PORT=3306

# Nombre de la base de datos de ImagineCRM
DB_NAME=imaginecrm

# Usuario con permisos de lectura (SELECT) en la base de datos
# IMPORTANTE: Usar un usuario de solo lectura por seguridad
DB_USER=metabase_readonly

# Contraseña del usuario de base de datos
DB_PASSWORD=contraseña_readonly_segura
